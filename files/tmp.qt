type Vect(Nat n, Ty t) {
    constructor Nil of Vect(0, t);
    constructor Cons(t x, Vect(n, t) xs) of Vect(n + 1, t);
}

type Matrix(Nat m, Nat n) {
    constructor MkMatrix(Vect(m, Vect(n, Nat)) vs);
}

func index(Matrix(m, n) a, Fin(m) i, Fin(n) j) returns Nat {
                              
}