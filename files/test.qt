func search (Nat n, Vect(n, Nat) v, Nat x) of Fin(n) {
    let Fin(n) i = 0; 
    let Fin(n) ret = n; 
    while ((finToNat(i)) < n) {
        if ((index(i, v)) == x) {
            ret = i;
        } else {;;}
        i = i + 1; 
    }
    return ret; 
}