func search (Nat n, Vect(n, Nat) v, Nat x) of Fin(n) {
    let Fin(n) i = 0; 
    let Nat ret = n; 
    while (i < n) {
        if ((index(i, ls)) == x) {
            ret = i;
        } else {
            ;;
        }
        i = i + 1; 
    }
    return ret; 
}